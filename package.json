{
  "name": "qrzzzz.github.io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Source for the Qrzzzz personal site.",
  "scripts": {
    "check": "npm run docs:test && npm run docs:check && npm run docs:output-test && npm run docs:build && npm run docs:navigation-test && npm run docs:check -- --dist",
    "docs:pull": "node scripts/pull-lyrics-card-docs.mjs",
    "docs:import": "node scripts/import-lyrics-card-docs.mjs",
    "docs:check": "node scripts/check-lyrics-card-docs.mjs",
    "docs:test": "node --test tests/*.test.mjs",
    "docs:output-test": "node scripts/test-vitepress-output.mjs",
    "docs:navigation-test": "node scripts/check-site-navigation.mjs",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "engines": {
    "node": ">=24 <25"
  },
  "devDependencies": {
    "minisearch": "^7.2.0",
    "vitepress": "1.6.4"
  }
}
